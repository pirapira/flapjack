# Permitted `native_decide` uses, as `<count> <path>`.
#
# `native_decide` discharges a goal by compiling it and trusting the result,
# which attaches a per-theorem axiom:
#
#   'viaNative' depends on axioms: [propext, Quot.sound, viaNative._native.native_decide.ax_1_1]
#   'viaKernel' depends on axioms: [propext, Quot.sound]
#
# This file is a ceiling, not a target. Counts may go down freely; any
# increase, or any use in a file not listed here, fails CI. When a count
# drops the check says so and asks for this file to be lowered.
#
# Before adding an entry, try in order: `decide`; then `decide +kernel`, which
# unfolds many `termination_by` definitions the elaborator's `whnf` stalls on;
# then `#guard <prop>` for an `example` or an uncited regression check, which
# keeps the check but produces no proof term and so needs no axiom.
#
# `decide +kernel` does not reach everything. It stalls on `compileExp`
# (Flapjack/PanToCrep.lean), which recurses with
# `termination_by expression => sizeOf expression`, so a goal that runs the
# compiler over a program containing an expression never gets the innermost
# `compileProg` pass to a constructor. `.move` programs carry no expression to
# recurse into and reduce fine. Bisecting by fuel distinguishes this from a
# resource limit: a stalled goal fails at fuel 1 just as it does at fuel 16.
#
# When no tactic reaches a goal, the question is not settled -- ask next
# whether the result needs to be a theorem. An uncited execution check does
# not, and `#guard` discharges it with no axiom (see #343).

# --- Cited pipeline shape theorems -------------------------------------------
# Each runs the compiler over a program and compares the result to a literal,
# and each is cited by a neighbouring proof via `rw [...]`, so the equation has
# to exist as a theorem -- `#guard` cannot stand in. No tactic reaches them:
# the term needs more unfolding of well-founded recursion than the kernel will
# do, and `simp` stalls on `compileFlapjackRiscV` itself.
5 Flapjack/Correctness.lean
3 Flapjack/Test/CorrectnessTarget.lean

# --- Named execution theorems ------------------------------------------------
# These run a linked image on the RISC-V machine (`executeFunctionAt`,
# `executeFunctionAtWithFfi`) or evaluate a compiled Loop program. Execution is
# past what kernel reduction will unfold, so `native_decide` is what lets these
# results be stated with a name at all.
3 Flapjack/Test/FullSsaPipeline.lean
1 Flapjack/Test/FullSsaCorrectness.lean
1 Flapjack/Test/FullSsaHandler.lean
1 Flapjack/Test/FullSsaFfiCall.lean
1 Flapjack/Test/FullSsaHandlerFfi.lean
1 Flapjack/Test/CorrectnessFfi.lean
1 Flapjack/Test/SourceToLoop.lean

# --- Declaration-driven parameter-shape regressions -------------------------
# These regressions execute the declaration compiler over Word expressions;
# kernel reduction stalls in `compileExp`, so the named correctness check and
# its two declaration cases must use `native_decide`.
1 Flapjack/FlatCorrectness.lean
2 Flapjack/Test/SourceDeclarations.lean

# --- Nested `have` inside a larger proof -------------------------------------
# Supplies a proof term to the surrounding proof, so `#guard` does not apply.
1 Flapjack/Test/AllocatorSpills.lean

# --- Source-to-Loop FFI agreement ------------------------------------------
# `sourceToLoop_ffi_simulation` relates Loop evaluation to Pan evaluation
# across an FFI call. Same `compileExp` stall as above; `decide` and
# `decide +kernel` both fail. Kept as a theorem rather than a `#guard`
# because it states an agreement between two semantics, not a single
# execution outcome -- the companion `sourceToLoop_ffi_executes` was the
# execution check and is now a `#guard`.
1 Flapjack/Test/SourceToLoopFfi.lean
